# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real keys.

# Required for the deterministic data/backtesting layer:
EODHD_API_KEY=

# Required later, once we build the agent layer:
OPENAI_API_KEY=
LANGSMITH_API_KEY=
